# Auto detect text files and perform LF normalization
* text=auto

# 소스 코드 파일들
*.java text diff=java
*.kt text diff=java
*.kts text diff=java
*.xml text
*.json text
*.txt text
*.properties text
*.gradle text diff=java
*.md text

# 스크립트 파일들
*.sh text eol=lf
*.bat text eol=crlf
gradlew text eol=lf
gradlew.bat text eol=crlf

# 바이너리 파일들 (일반 Git, LFS 아님)
*.jar binary
*.apk binary
*.so binary
*.dll binary
*.exe binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary

# 빌드 로그는 바이너리로 처리
*.log binary
.ninja_log binary